async function loadBlog(){const list=document.getElementById("blogList");if(!list)return;try{const res=await fetch("blog-data.json");const a=await res.json();list.innerHTML=a.map(x=>`<article class="blog-card"><div class="blog-card-thumb">${x.emoji||"📝"}</div><div class="blog-card-body"><span class="blog-category">${x.category}</span><h2>${x.title}</h2><p>${x.summary}</p><div class="blog-date">${x.date}</div><a class="blog-read" href="${x.url}">記事を読む</a></div></article>`).join("")}catch(e){list.innerHTML="<p>記事データを読み込めませんでした。</p>"}}loadBlog();