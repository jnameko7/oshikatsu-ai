async function loadArticle(){const params=new URLSearchParams(location.search);const id=params.get('id');const mount=document.getElementById('articleDetail');if(!id){mount.innerHTML='<p>記事が見つかりません。</p>';return;}const res=await fetch('/api/articles');const data=await res.json();const article=data.contents.find(x=>x.id===id);if(!article){mount.innerHTML='<p>記事が見つかりません。</p>';return;}mount.innerHTML=`<article>${article.thumbnail?.url?`<img src="${article.thumbnail.url}" style="width:100%;border-radius:20px">`:''}<h1>${article.title}</h1><p>${article.description||''}</p><div>${article.body||''}</div></article>`;} loadArticle();